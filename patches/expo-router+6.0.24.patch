diff --git a/node_modules/expo-router/build/ExpoRoot.js b/node_modules/expo-router/build/ExpoRoot.js
index 4bef114..dcde96c 100644
--- a/node_modules/expo-router/build/ExpoRoot.js
+++ b/node_modules/expo-router/build/ExpoRoot.js
@@ -66,6 +66,7 @@ const documentTitle = {
  * @hidden
  */
 function ExpoRoot({ wrapper: ParentWrapper = react_1.Fragment, ...props }) {
+    console.log('[boot-diag] ExpoRoot() called');
     /*
      * Due to static rendering we need to wrap these top level views in second wrapper
      * View's like <SafeAreaProvider /> generate a <div> so if the parent wrapper
@@ -93,6 +94,7 @@ const initialUrl = react_native_1.Platform.OS === 'web' && typeof window !== 'un
     ? new URL(window.location.href)
     : undefined;
 function ContextNavigator({ context, location: initialLocation = initialUrl, wrapper: WrapperComponent = react_1.Fragment, linking = {}, }) {
+    console.log('[boot-diag] ContextNavigator() called');
     // location and linking.getInitialURL are both used to initialize the router state
     //  - location is used on web and during static rendering
     //  - linking.getInitialURL is used on native
@@ -125,8 +127,11 @@ function ContextNavigator({ context, location: initialLocation = initialUrl, wra
     const serverUrl = serverContext.location
         ? `${serverContext.location.pathname}${serverContext.location.search}`
         : undefined;
+    console.log('[boot-diag] ContextNavigator: calling useStore()');
     const store = (0, router_store_1.useStore)(context, linking, serverUrl);
+    console.log('[boot-diag] ContextNavigator: useStore() returned');
     (0, useDomComponentNavigation_1.useDomComponentNavigation)();
+    console.log('[boot-diag] ContextNavigator: useDomComponentNavigation() returned, shouldShowTutorial=', store.shouldShowTutorial());
     if (store.shouldShowTutorial()) {
         SplashScreen.hideAsync();
         if (process.env.NODE_ENV === 'development') {
@@ -151,6 +156,7 @@ function ContextNavigator({ context, location: initialLocation = initialUrl, wra
     </storeContext_1.StoreContext.Provider>);
 }
 function Content() {
+    console.log('[boot-diag] Content() called');
     const children = [<primitives_1.Screen name={constants_1.INTERNAL_SLOT_NAME} component={router_store_1.store.rootComponent}/>];
     if ((0, utils_1.shouldAppendNotFound)()) {
         children.push(<primitives_1.Screen name={constants_1.NOT_FOUND_ROUTE_NAME} component={Unmatched_1.Unmatched}/>);
diff --git a/node_modules/expo-router/build/global-state/router-store.js b/node_modules/expo-router/build/global-state/router-store.js
index 05ad58f..201a1c6 100644
--- a/node_modules/expo-router/build/global-state/router-store.js
+++ b/node_modules/expo-router/build/global-state/router-store.js
@@ -133,11 +133,15 @@ exports.store = {
     },
 };
 function useStore(context, linkingConfigOptions, serverUrl) {
+    console.log('[boot-diag] useStore: start');
     const navigationRef = (0, native_1.useNavigationContainerRef)();
+    console.log('[boot-diag] useStore: got navigationRef');
     const config = expo_constants_1.default.expoConfig?.extra?.router;
+    console.log('[boot-diag] useStore: got config from expo-constants');
     let linking;
     let rootComponent = react_1.Fragment;
     let initialState;
+    console.log('[boot-diag] useStore: calling getRoutes()');
     const routeNode = (0, getRoutes_1.getRoutes)(context, {
         ...config,
         skipGenerated: true,
@@ -145,6 +149,7 @@ function useStore(context, linkingConfigOptions, serverUrl) {
         platform: react_native_1.Platform.OS,
         preserveRedirectAndRewrites: true,
     });
+    console.log('[boot-diag] useStore: getRoutes() returned, routeNode=', !!routeNode);
     const redirects = [config?.redirects, config?.rewrites]
         .filter(Boolean)
         .flat()
@@ -156,6 +161,7 @@ function useStore(context, linkingConfigOptions, serverUrl) {
         ];
     });
     if (routeNode) {
+        console.log('[boot-diag] useStore: calling getLinkingConfig()');
         // We have routes, so get the linking config and the root component
         linking = (0, getLinkingConfig_1.getLinkingConfig)(routeNode, context, () => exports.store.getRouteInfo(), {
             metaOnly: linkingConfigOptions.metaOnly,
@@ -165,7 +171,9 @@ function useStore(context, linkingConfigOptions, serverUrl) {
             sitemap: config?.sitemap ?? true,
             notFound: config?.notFound ?? true,
         });
+        console.log('[boot-diag] useStore: getLinkingConfig() returned, calling getQualifiedRouteComponent()');
         rootComponent = (0, useScreens_1.getQualifiedRouteComponent)(routeNode);
+        console.log('[boot-diag] useStore: getQualifiedRouteComponent() returned');
         // By default React Navigation is async and does not render anything in the first pass as it waits for `getInitialURL`
         // This will cause static rendering to fail, which once performs a single pass.
         // If the initialURL is a string, we can prefetch the state and routeInfo, skipping React Navigation's async behavior.
diff --git a/node_modules/expo-router/build/qualified-entry.js b/node_modules/expo-router/build/qualified-entry.js
index dbf812f..38ad970 100644
--- a/node_modules/expo-router/build/qualified-entry.js
+++ b/node_modules/expo-router/build/qualified-entry.js
@@ -17,6 +17,7 @@ const head_1 = require("./head");
 require("./fast-refresh");
 // Must be exported or Fast Refresh won't update the context
 function App() {
+    console.log('[boot-diag] qualified-entry App() called');
     return (<head_1.Head.Provider>
       <ExpoRoot_1.ExpoRoot context={_ctx_1.ctx}/>
     </head_1.Head.Provider>);
